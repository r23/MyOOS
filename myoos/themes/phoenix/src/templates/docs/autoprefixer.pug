extends ../partials/layout-docs

//- Override default config variables
append config
  - var pageTitle = 'Browser Support'
  - var isNestedFile = true
  - var breadcrumbs = [{'title': 'Home', 'link': '../index.html'}, {'title': 'Docs', 'link': '#'}]

//- Page Content
block content
  .pb-5.mb-2
    p Vendor prefixes are not used inside SCSS instead we use <a href='https://github.com/postcss/autoprefixer' class='font-weight-semibold' target='_blank'>Autoprefixer</a> to handle intended browser support via CSS prefixes. After compilation CSS styles are passed through Autoprefixer to add necessary vendor prefixes. As you know vendor prefixes are important to ensure cross-browser compatibility of latets CSS3 features.

    h2.h4.pt-3 Customize browser support via Autoprefixer settings
    p Autoprefixer is integral part of Gulp building process. So to customize its settings you need to go to <code>gulpfile.js</code> and find all instances of <code>.pipe(autoprefixer({{ ... })</code>. You will see <code>browsers</code> setting. It's currently set to <code>['last 2 versions', 'ie 11']</code> which reflects the browser set we retrieved from our Google analytics. Generally it's a good practice to collect some analytics about what Browsers/OSs your visitors use. And then customize Autoprefixer settings based on this data.
    p For more information about available Browserlist options visit <a href='https://github.com/browserslist/browserslist' class='font-weight-medium' target='_blank'>https://github.com/browserslist/browserslist</a>.
    img(src='../img/docs/autoprefixer.png' alt='Autoprefixer').d-block.img-thumbnail.mt-4    